CREATE DATABASE Projetos;

-- Antes de cada comando selecionar e executar esse comando: "USE nome-do-banco"
USE Projetos;
SELECT * FROM usuarios;

CREATE TABLE USUARIOS (
ID INT AUTO_INCREMENT PRIMARY KEY,
NOME VARCHAR(100) NOT NULL,
EMAIL VARCHAR(100) NOT NULL UNIQUE,
SENHA VARCHAR(50) NOT NULL
);

CREATE  TABLE IDEIAS (
ID INT AUTO_INCREMENT PRIMARY KEY,
USUARIOS_ID INT,
TITULO VARCHAR(100),
DESCRICAO TEXT NOT NULL,
ANONIMO BOOLEAN DEFAULT FALSE,
FOREIGN KEY(USUARIOS_ID) REFERENCES USUARIOS(ID)
);

CREATE TABLE COMENTARIOS (
ID INT AUTO_INCREMENT PRIMARY KEY,
IDEIAS_ID INT,
USUARIOS_ID INT NOT NULL,
COMENTARIOS TEXT NOT NULL,
FOREIGN KEY (IDEIAS_ID) REFERENCES IDEIAS(ID),
FOREIGN KEY(USUARIOS_ID) REFERENCES USUARIOS(ID)
);

CREATE TABLE RASCUNHO (
ID INT AUTO_INCREMENT PRIMARY KEY,
USUARIOS_ID INT NOT NULL,
TITULO TEXT NOT NULL,
DESCRICAO TEXT NOT NULL,
FOREIGN KEY (USUARIOS_ID)
REFERENCES USUARIOS(ID)
);

CREATE TABLE CURTIDAS (
    ID INT AUTO_INCREMENT PRIMARY KEY,
    USUARIOS_ID INT NOT NULL,
    IDEIAS_ID INT NOT NULL,

    FOREIGN KEY (USUARIOS_ID) REFERENCES USUARIOS(ID),
    FOREIGN KEY (IDEIAS_ID) REFERENCES IDEIAS(ID)
);


-- COMO EDITAR O NOME DO CAMPO DA TABELA NO MSQL:
-- ALTER TABLE nome_da_tabela RENAME COLUMN nome_antigo TO novo_nome;
-- ALTER TABLE IDEIAS RENAME COLUMN E_ANONIMO TO ANONIMO;